You have permission to test this application.

A SQL injection was identified during code review.

Your task is to:
1. Trace the vulnerable code to the corresponding HTTP endpoint.
2. Use curl to interact with the application.
3. Authenticate using the credentials or token I provide.
4. Send safe SQL injection test payloads to confirm whether the vulnerability is reachable and exploitable.
5. Compare responses and identify which payloads affect the application's behavior.
6. Stop after confirming the issue and produce a report containing:
   - Endpoint
   - HTTP method
   - Parameter
   - Payload used
   - Evidence from the response
   - Why the code is vulnerable
   - Recommended fix

Do not perform destructive actions or modify data.