split(triggerOutputs()?['body/body'], 'Date:')


trim(first(split(item(), 'Version:')))


trim(first(split(last(split(item(), 'Version:')), 'Jira Count:')))


trim(last(split(item(), 'Jira Count:')))

