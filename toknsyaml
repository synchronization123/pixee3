INPUT GATE

The first and only action is requesting the Excel file path.

Ask exactly:

Enter the full absolute path of the Excel file containing Jira IDs.

Wait for the user response.

Do not perform planning, analysis, or tool calls until the path is provided.

AFTER USER INPUT

Store the input as:

EXCEL_FILE_PATH

EXECUTION MODE

Run silently.

Do not explain actions.
Do not print reasoning.
Do not communicate with the user again.

Only update the Excel file.

When finished output only:

Execution completed.

PROCESS

LOAD EXCEL

df = pandas.read_excel(EXCEL_FILE_PATH)

VERIFY COLUMNS

Required columns:

Jira
Suggestion
Reason
Notes

FOR EACH ROW

CURRENT_JIRA = df["Jira"][row_index]

SKIP IF ALREADY PROCESSED

IF df["Suggestion"][row_index] is not empty

CONTINUE

FETCH DATA

JIRA_DATA = titanium-ai.get_jira_issue(CURRENT_JIRA)

PR_DATA = titanium-ai.get_bitbucket_pr(CURRENT_JIRA)

PR_CHANGES = titanium-ai.get_bitbucket_pr_changes(PR_DATA)

UNDERSTAND DIFF

Identify modified files and change type.

FUNCTIONAL REVIEW

Check for:

logic errors
null issues
incorrect conditions
exception handling issues
API misuse
database query errors

SECURITY REVIEW

Check for:

authentication changes
authorization logic
access control
input validation
token/session handling
cryptography usage
configuration changes

VULNERABILITY DETECTION

Possible types:

Broken Access Control
Injection
Sensitive Data Exposure
Hardcoded Secrets
Privilege Escalation
Weak Cryptography

DECISION

IF security-sensitive logic modified

Suggestion = "Requires Manual Security Testing"
Reason = "Security-sensitive code modified."

ELSE IF change only UI or refactor

Suggestion = "No Security Testing Required"
Reason = "No security impact."

ELSE

Suggestion = "Needs Further Security Review"
Reason = "Security impact unclear."

NOTES

IF vulnerability detected

Notes = "Potential vulnerability detected: " + VULNERABILITY_TYPE

ELSE

Notes = "No vulnerability detected."

UPDATE EXCEL

df.loc[row_index,"Suggestion"] = Suggestion
df.loc[row_index,"Reason"] = Reason
df.loc[row_index,"Notes"] = Notes

SAVE CHECKPOINT

df.to_excel(EXCEL_FILE_PATH, index=False)