import yaml
from pathlib import Path

CONFIG_PATH = Path("C:/mcp/tokens.yaml")

def load_config():
    if not CONFIG_PATH.exists():
        raise FileNotFoundError("tokens.yaml not found")

    with open(CONFIG_PATH, "r") as f:
        return yaml.safe_load(f)