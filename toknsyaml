STEP 1 — INPUT

Ask exactly:

Enter the full absolute path of the Excel file containing Jira IDs.

Wait for user input.

Store the input as:

EXCEL_FILE_PATH

STEP 2 — LOAD EXCEL

Use pandas to read the Excel file.

df = pandas.read_excel(EXCEL_FILE_PATH)

Verify the Excel contains columns:

Jira
Suggestion

If columns are missing, stop execution.

STEP 3 — ITERATION SETUP

TOTAL_ROWS = number of rows in df

STEP 4 — PROCESS JIRA ROWS

FOR row_index from 0 to TOTAL_ROWS - 1

CURRENT_JIRA = df["Jira"][row_index]

STEP 5 — FETCH JIRA

Call:

titanium-ai.get_jira_issue(CURRENT_JIRA)

Store response as:

JIRA_DATA

STEP 6 — FETCH PULL REQUEST

Call:

titanium-ai.get_bitbucket_pr(CURRENT_JIRA)

Store response as:

PR_DATA

STEP 7 — FETCH CODE CHANGES

Call:

titanium-ai.get_bitbucket_pr_changes(PR_DATA)

Store response as:

PR_CHANGES

STEP 8 — SECURITY ANALYSIS

Analyze PR_CHANGES.

STEP 9 — DECISION

IF authentication, authorization, access control, input validation, or security logic changed

Suggestion = "Requires Manual Security Testing"

ELSE IF change is UI, documentation, or non-security refactor

Suggestion = "No Security Testing Required"

ELSE

Suggestion = "Needs Further Security Review"

STEP 10 — UPDATE EXCEL

df.loc[row_index, "Suggestion"] = Suggestion

STEP 11 — CONTINUE

Repeat for next Jira.

STEP 12 — SAVE FILE

df.to_excel(EXCEL_FILE_PATH, index=False)