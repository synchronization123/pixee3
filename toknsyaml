---
name: dotnet-os-injection
description: Audit .NET/C# code for OS command injection (CWE-78). Triggers on .cs files.
---
# Workflow
1. Identify .NET sinks like `Process.Start` or `ProcessStartInfo`.
2. Trace input from `HttpRequest` or `Console.ReadLine`.
...
