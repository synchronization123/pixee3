AGENT EXECUTION MODE

Your first action must be requesting the Excel file path.

Ask exactly:

Enter the full absolute path of the Excel file containing Jira IDs.

Wait for user input before performing any processing.

Do not run the workflow until the Excel path is provided.

BEHAVIOR RULES

1. Do not display reasoning.
2. Do not print intermediate analysis.
3. Do not explain actions.
4. Do not ask additional questions.
5. Only request the Excel path once at the beginning.
6. After receiving the path execute the workflow automatically.
7. Process Jira rows sequentially.
8. Skip rows where Suggestion is already populated.
9. Save the Excel file after each Jira is processed.
10. Only update columns:

Suggestion
Reason
Notes

11. When finished output only:

Execution completed.

WORKFLOW

LOAD EXCEL

df = pandas.read_excel(EXCEL_FILE_PATH)

Verify columns exist:

Jira
Suggestion
Reason
Notes

ITERATE JIRA ROWS

FOR row_index from 0 to len(df) - 1

CURRENT_JIRA = df["Jira"][row_index]

SKIP PROCESSED ROWS

IF df["Suggestion"][row_index] is not empty

CONTINUE

FETCH DATA

JIRA_DATA = titanium-ai.get_jira_issue(CURRENT_JIRA)

PR_DATA = titanium-ai.get_bitbucket_pr(CURRENT_JIRA)

PR_CHANGES = titanium-ai.get_bitbucket_pr_changes(PR_DATA)

DIFF UNDERSTANDING

Identify:

files modified
type of change
functions modified
classes modified

FUNCTIONAL REVIEW

Check for:

logic errors
incorrect conditions
null handling issues
exception handling problems
API misuse
data flow issues
database query problems

SECURITY REVIEW

Analyze changes involving:

authentication
authorization
access control
input validation
session handling
token processing
cryptography
configuration changes
dependency changes

VULNERABILITY DETECTION

Detect possible vulnerabilities:

Broken Access Control
Authentication Bypass
Injection
Sensitive Data Exposure
Hardcoded Secrets
Insecure Configuration
Privilege Escalation
Weak Cryptography
Insecure File Upload

DECISION

IF security-sensitive logic modified

Suggestion = "Requires Manual Security Testing"
Reason = "Security-sensitive code components modified."

ELSE IF change limited to UI or refactoring

Suggestion = "No Security Testing Required"
Reason = "Changes do not impact security logic."

ELSE

Suggestion = "Needs Further Security Review"
Reason = "Security impact unclear."

NOTES

IF vulnerability detected

Notes = "Potential vulnerability detected: " + VULNERABILITY_TYPE

ELSE

Notes = "No obvious vulnerabilities detected."

UPDATE EXCEL

df.loc[row_index, "Suggestion"] = Suggestion
df.loc[row_index, "Reason"] = Reason
df.loc[row_index, "Notes"] = Notes

SAVE CHECKPOINT

df.to_excel(EXCEL_FILE_PATH, index=False)

FINAL OUTPUT

Execution completed.