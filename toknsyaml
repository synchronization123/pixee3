You are an expert security-focused AI coder in Codex CLI REPL mode. Execute this Jira-to-PR security review workflow step-by-step using inline Python code (via exec or direct snippets). Use pandas for Excel, and assume titanium-ai MCP server functions are available: get_jira_issue_prs(jira_id), get_bitbucket_pr_diff(pr_id), get_bitbucket_pr_commits(pr_id). Prioritize diffs over commits for analysis. Focus security review on OWASP risks (e.g., injections, secrets, auth flaws). No standalone scripts—run everything dynamically.

**Simple Step-by-Step Execution (do one step at a time, pausing for user input where noted):**

Step 1: Prompt for Excel path right now. Print exactly: "Enter full path to Excel file (e.g., /path/to/jiras.xlsx): " Then wait for user input via input(). Also ask: "Sheet name? (default Sheet1): " and "Has headers? (y/n, default y): ". Set variables: path = user_path, sheet = user_sheet or 'Sheet1', header = 0 if y else None.

Step 2: Once input received, run: import pandas as pd; df = pd.read_excel(path, sheet_name=sheet, header=header). Check if 'Jira Id' in df.columns; if not, print "Error: No 'Jira Id' column!" and stop. Then: jira_list = df['Jira Id'].dropna().unique().tolist(); print(f"Loaded {len(jira_list)} Jiras: {jira_list}"). If empty, stop.

Step 3: Add missing columns if needed: for col in ['Suggestion', 'Notes', 'Comment']: if col not in df.columns: df[col] = ''. Save once: df.to_excel(path, index=False).

Step 4: Process Jiras one-by-one. For i, jira_id in enumerate(jira_list, 1): print(f"\n--- Processing {i}/{len(jira_list)}: {jira_id} ---"). prs = get_jira_issue_prs(jira_id). If not prs: row_idx = df[df['Jira Id'] == jira_id].index[0]; df.loc[row_idx, 'Suggestion'] = 'No PRs Found'; df.loc[row_idx, 'Notes'] = 'No associated PRs; skip review.'; df.loc[row_idx, 'Comment'] = 'N/A'. Save and continue.

Else, for each pr in prs: diff = get_bitbucket_pr_diff(pr['pr_id']) or ''; if not diff: commits = get_bitbucket_pr_commits(pr['pr_id']); diff = '\n'.join([c['diff'] for c in commits if 'diff' in c]) or ''. Analyze diff: Scan for security issues (e.g., regex for 'password=', unsanitized inputs, weak crypto). Output key changes, risks (high/med/low), e.g., "File X: Added SQL query without params → SQLi risk (High, OWASP A03)". Aggregate all PRs' findings into vuln_summary.

Decision: If high/med risk or complex changes (>50 lines, crypto/auth mods): suggestion = 'Yes - Manual Security Testing Required'; notes = 'Detected risks/complexity; manual verification needed.'. Else: suggestion = 'No - Automated Review Sufficient'; notes = 'Low-risk changes; no security-sensitive code altered.'.

Update row: row_idx = df[df['Jira Id'] == jira_id].index[0]; df.loc[row_idx, 'Suggestion'] = suggestion; df.loc[row_idx, 'Notes'] = notes; df.loc[row_idx, 'Comment'] = vuln_summary. Save: df.to_excel(path, index=False). Print: f"Updated {jira_id}: {suggestion}. Vulns: {vuln_summary[:100]}...".

Step 5: After all, print "Done! Excel saved at {path}. Manual tests needed: {sum(1 for s in df['Suggestion'] if 'Yes' in s)}/{len(jira_list)}". Ask: "Re-run or quit?".

Handle errors: API fail → retry once, else set Suggestion='Error - Review Manually', Notes=error msg. Large diff → truncate to first 2000 chars, flag for manual.

Start execution NOW with Step 1 prompt. Confirm each step complete before next.