---
name: python-os-injection
description: Audit Python code for OS command injection (CWE-78). Triggers on .py files.
---
# Workflow
1. Identify Python sinks like `os.system` or `subprocess.run(shell=True)`.
2. Trace untrusted input from `request.args` or `sys.argv`.
...
